server {
    listen 80;
    server_name jet-way.duckdns.org;
    server_tokens off;

    location /.well-known/acme-challenge/ {
      root var/www/certbot;
    }

    location / {
      root   /usr/share/nginx/html;
      index  index.html index.htm;
      try_files $uri $uri/ /index.html;
      # return 301 https://jet-way.duckdns.org;
    } 

    error_page 404 /index.html;
    location = / {
      root /usr/share/nginx/html;
      internal;
    }
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
      root   /usr/share/nginx/html;
    }
  }

  # server {
  #   listen 443 default_server ssl http2;
  #   server_name jet-way.duckdns.org;
  #   ssl_certificate /etc/nginx/ssl/live/jet-way.duckdns.org/fullchain.pem;
  #   ssl_certificate_key /etc/nginx/ssl/live/jet-way.duckdns.org/privkey.pem;

  #   location / {
  #     root   /usr/share/nginx/html;
  #     index  index.html index.htm;
  #     try_files $uri $uri/ /index.html;
  #   } 
  #   error_page 404 /index.html;
  #   location = / {
  #     root /usr/share/nginx/html;
  #     internal;
  #   }
  #   error_page   500 502 503 504  /50x.html;
  #   location = /50x.html {
  #     root   /usr/share/nginx/html;
  #   }
  # }